name = "cinacoin-website"
compatibility_date = "2025-01-01"

[build]
command = "pnpm install --frozen-lockfile && pnpm --filter cinacoin-website build"
cwd = "../.."
output_directory = "apps/website/out"
