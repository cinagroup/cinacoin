# Standalone demo.cinacoin.com root -> the basePath the app is built under.
# (The app is served at /demo so it can also mount at cinacoin.com/demo
#  via the consolidation router Worker.)
/  /demo/  301
