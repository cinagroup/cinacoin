# Standalone wallet.cinacoin.com root -> the basePath the app is built under.
# (The app is served at /wallets so it can also mount at cinacoin.com/wallets
#  via the consolidation router Worker.)
/  /wallets/  301
