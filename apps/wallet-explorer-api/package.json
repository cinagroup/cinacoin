{
  "name": "wallet-explorer-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cinacoin Wallet Explorer API — Hono + Cloudflare Workers API for wallet registry queries",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx,.js,.jsx",
    "test": "vitest run"
  },
  "dependencies": {
    "@cinacoin/wallet-registry": "workspace:*",
    "hono": "^4.6.0",
    "hono-rate-limiter": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250000.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.90.0"
  }
}
