# Standalone analytics.cinacoin.com root -> the basePath the app is built under.
# (Served at /analytics so it can also mount at cinacoin.com/analytics via
#  the consolidation router Worker.)
/  /analytics/  301
